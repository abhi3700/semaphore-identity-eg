{
  "name": "semaphore-identity-eg",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "idd": "npx tsc && node src/id_deterministic.js",
    "idr": "npx tsc && node src/id_random.js",
    "group:offchain": "npx tsc && node src/group_offchain.js"
  },
  "dependencies": {
    "@semaphore-protocol/group": "^3.15.0",
    "@semaphore-protocol/identity": "^3.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.4",
    "typescript": "^5.3.2"
  }
}
