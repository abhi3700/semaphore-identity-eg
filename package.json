{
  "name": "semaphore-playground",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "id:random": "npx tsc && node src/id_random.js",
    "id:deterministic": "npx tsc && node src/id_deterministic.js",
    "group:offchain": "npx tsc && node src/group_offchain.js",
    "group:offchain-depth": "npx tsc && node src/group_offchain_depth.js",
    "group:offchain-members": "npx tsc && node src/group_offchain_members.js"
  },
  "dependencies": {
    "@semaphore-protocol/contracts": "^3.15.1",
    "@semaphore-protocol/group": "^3.15.0",
    "@semaphore-protocol/identity": "^3.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.4",
    "typescript": "^5.3.2"
  }
}
